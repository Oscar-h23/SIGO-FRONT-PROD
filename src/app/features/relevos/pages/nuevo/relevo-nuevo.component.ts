import{Component,OnInit}from'@angular/core';import{FormsModule}from'@angular/forms';
import { AuthService } from '../../../../core/auth/auth.service';
import{RelevoApiService}from'../../services/relevo-api.service';import{ElementoRelevo,Via}from'../../models/relevo.models';@Component({selector:'app-relevo-nuevo',standalone:true,imports:[FormsModule],templateUrl:'./relevo-nuevo.component.html',styleUrl:'../../../../shared/page.css'})export class RelevoNuevoComponent implements OnInit{turno='A';observacion='';elementos:ElementoRelevo[]=[];vias:Via[]=[];mensaje='';constructor(public auth:AuthService,private api:RelevoApiService){}ngOnInit(){this.api.getElementos().subscribe(x=>this.elementos=x);const p=this.auth.usuario()?.plazaId;if(p)this.api.getVias(p).subscribe(x=>this.vias=x)}guardar(){const u=this.auth.usuario();if(!u?.plazaId)return;const checklist=this.elementos.map(e=>({elementoId:e.id,estado:'OPERATIVO',detalle:null}));const vias=this.vias.map(v=>({viaId:v.id,estado:'OPERATIVO',detalle:null}));this.api.registrar({plazaId:u.plazaId,turno:this.turno,fecha:new Date().toISOString().slice(0,10),observacion:this.observacion||null,checklist,vias}).subscribe({next:()=>this.mensaje='Relevo registrado.',error:e=>this.mensaje=e?.error?.message??'No se pudo registrar el relevo.'})}}